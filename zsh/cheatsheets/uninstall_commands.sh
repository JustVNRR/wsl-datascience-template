# ==========================================
# OPTIONAL TOOLING — THE WAY OUT
# ==========================================
# requires: gcloud
#
# Offered only while the CLI is installed: the counterpart of
# install_commands.sh, and the same rule as the gmake menu - the way out is
# never offered before the way in.

gmake gcp_uninstall                          # Uninstall the Google Cloud CLI (frees ~409 MB, keeps your gcloud logins)
